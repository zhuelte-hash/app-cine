<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base -->
    <style name="Base.Theme.AppCine" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <!-- Tema principal -->
    <style name="Theme.AppCine" parent="Base.Theme.AppCine"/>

    <!-- Botón primero -->
    <style name="btnPrimero" parent="Theme.AppCine">
        <item name="backgroundTint">@color/primer</item>
        <item name="cornerRadius">0dp</item>
    </style>

    <!-- Botón segundo -->
    <style name="btnSegundo" parent="Theme.AppCine">
        <item name="backgroundTint">@color/segundo</item>
    </style>

    <!-- Botón tercero -->
    <style name="btnTercero" parent="Theme.AppCine">
        <item name="backgroundTint">@color/tercero</item>
    </style>

</resources>
